public enum Color {
    /* 
     * 封閉, store value
     * 有限數量
     * can call 到佢地
     * Example: weekdays, mon, tue ...
     * Example: currency, USD, HKD, ...
     * 如果要用佢, RED YELLOW BLUE 就係一個 object
     * Color.RED
     * Color.YELLOW
     */

     RED, 
     YELLOW, 
     BLUE,//有 comma, 下次 insert 就會唔使改兩行
     ;

     
}
