public enum Color {
    /* 
     * 封閉, store value
     * 有限數量
     * can call 到佢地
     * Example: weekdays, mon, tue ...
     * Example: currency, USD, HKD, ...
     */

     RED, 
     YELLOW, 
     BLUE,//有 comma, 下次 insert 就會唔使改兩行
     ;

     
}
